# 更新日志

[English](CHANGELOG.md) | [简体中文](CHANGELOG.zh-CN.md)

本文件记录该项目的所有重要变更。

格式参考 Keep a Changelog 的实践原则。

## [1.0.2] - 2026-07-27

本次为补丁版本，重点优化多语言体验与弹窗布局稳定性。

### 新增
- 新增 6 种语言运行时支持：English、简体中文、繁體中文、Español、Français、Deutsch。
- 在弹窗头部新增手动语言切换（带国旗标识）能力。
- 新增繁体中文语言包（`zh-TW`）。

### 变更
- 优化弹窗头部布局：语言选择框与标题放在同一行。
- 繁体中文选项国旗调整为香港旗，便于快速识别。
- 补齐中文界面中残留英文文案与方向字段翻译。

### 修复
- 修复语言切换时弹窗界面抖动问题：固定弹窗尺寸并改为内部滚动，减少切换跳动。

## [1.0.1] - 2026-07-27

本次为补丁版本，重点提升导出稳定性与打包流程。

### 修复
- 修复了 `fixed` 和 `sticky` 悬浮层遮挡导出 PDF 顶部内容的问题。
- 在打印前临时隐藏疑似遮挡层，并在导出后自动恢复页面样式。

### 新增
- 新增一键 CRX 打包脚本。
- 新增 macOS 下浏览器自动探测回退逻辑，用于 CRX 打包。

## [1.0.0] - 2026-07-22

首个公开发布版本。

### 新增
- 初始 Chrome 扩展（Manifest V3），支持将当前标签页导出为单页 PDF。
- 弹窗 UI，支持直接导出与高级区域参数控制（`left`、`right`、`top`、`bottom`）。
- 可视化滚动选区模式，使用与视口对齐的绿色高亮边框。
- 在选区模式点击 Done 后可立即触发导出。
- 后台导出链路基于 Chrome DevTools Protocol（`Page.printToPDF`），并包含临时裁剪与恢复逻辑。
- 可选“保存前询问文件名（Ask file name before save）”开关。
- 完整图标与 Chrome Web Store 上架素材（截图、横幅、元数据、隐私/支持页面）。

### 变更
- 优化选区调整行为：向上滚动时优先收缩 `bottom`，交互更直观。
- 选区覆盖层约束在内容视口内，避免与浏览器 UI 产生视觉重叠。
